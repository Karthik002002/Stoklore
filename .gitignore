.venv
__pycache__/
*.pyc

# stale one-off artifact from before the dashboard was replaced by the React app
dashboard.html

*.log
.DS_Store

frontend/node_modules
frontend/dist

# manual-trade screenshot uploads (api.py) - local-only, never shared/committed
uploads/

# local-only: LiteLLM proxy connection + guardrails config (ports, model list, env var names
# specific to this machine's setup) - never shared/committed, each dev configures their own
litellm.config.yaml
test_guardrails.py


.env

# graphify - generated knowledge-graph output (graph.json, GRAPH_REPORT.md, HTML viz, Obsidian
# vault, cost tracker) - regenerable from source, never committed
graphify-out/

.obsidian/

# local_data/ - scratch output of moneycontrol_local.py's standalone CLI mode (a personal ad-hoc
# fetch, separate from the app's own price_history_max collection) - fetched data, not code, so
# it stays out of git the same way uploads/ does.
local_data/

# per-machine MCP server config (local absolute paths) - never shared/committed
.mcp.json
fastapi==0.139.2
uvicorn==0.51.0
pydantic==2.13.4

psycopg[binary]==3.3.4

yfinance==1.5.1
beautifulsoup4==4.15.0
requests==2.34.2
ddgs==9.14.4

pandas==3.0.3
transformers==5.14.1
torch==2.13.0

# LiteLLM proxy CLI - optional, only needed for the litellm/* model backend (see Settings > LiteLLM)
litellm[proxy]==1.93.0
# LiteLLM's Langfuse logging callback (litellm_settings.success_callback in litellm.config.yaml)
# imports this directly - optional, only needed if that callback is enabled.
# Pinned <3: litellm 1.93.0's callback reads langfuse.version.__version__, a submodule the SDK
# removed in its v3 OTel-based rewrite, so 3.x/4.x break the callback outright.
langfuse==2.60.9
